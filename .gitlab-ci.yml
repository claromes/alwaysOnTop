image: node

build:
  stage: build

  artifacts:
    name: "alwaysontop_0.1.0_amd64.deb"
    when: always
    expire_in: never
    paths:
      - ./dist/installers/

  script:
    - npm install npm@latest -g
    - npm install
    - npm run linux64
    - npm run deb64
